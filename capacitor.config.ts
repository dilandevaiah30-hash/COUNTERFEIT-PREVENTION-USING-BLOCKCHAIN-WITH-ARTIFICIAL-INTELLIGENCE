import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.productverifier',
  appName: 'ProductVerifierCIT',
  webDir: 'build'
};

export default config;
